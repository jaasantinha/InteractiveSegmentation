# InteractiveSegmentation
Horos plugin for interactive segmentation using binary thresholding. 

The user can change upper and lower thresholds using the sliders and changes are applied automatically to the image. 

The result of the binary thresholding is shown using a 3D Brush ROI
